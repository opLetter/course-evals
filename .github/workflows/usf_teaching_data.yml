name: "USF: Update Teaching Data"
on:
  workflow_dispatch:
  schedule:
    - cron: "0 22 * * 1-5"
jobs:
  get-and-commit:
    uses: opletter/course-evals/.github/workflows/update_teaching_data.yml@master
    with:
      command: ./gradlew colleges:usf:run
      college: USF
